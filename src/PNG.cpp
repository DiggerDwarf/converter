#include "PNG.h"

image_data png::read(std::string filename)
{
    if (verbose) std::puts("Not available");
    std::exit(4);
}

void png::write(std::string filename, image_data* data)
{
    if (verbose) std::puts("Not available");
    std::exit(4);
}
